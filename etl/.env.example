# ============================================================
# NovaRetail Analytics — ETL Service
# Copia este archivo como .env y completa los valores
# ============================================================

# Conexión a PostgreSQL (mismo servidor que la API)
DATABASE_URL=postgresql://usuario:contrasena@localhost:5432/novaretail
